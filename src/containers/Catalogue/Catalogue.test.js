import { render, screen } from "@testing-library/react";
import Catalogue from "./Catalogue";

xdescribe("inital test for react card", () => {
  it("should render headings", () => {
    render(<Catalogue />)
    
  });
});